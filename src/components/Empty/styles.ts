import styled from "styled-components";

export const Wrapper = styled.div`
  display: flex;

  h1 {
    font-weight: 400;
    margin-right: 2rem;
    margin-bottom: 0;
    font-size: 2.4rem;
  }

  svg {
    fill: #ffd733;
  }
`;
